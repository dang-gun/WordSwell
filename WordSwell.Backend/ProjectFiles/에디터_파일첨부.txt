
1. 작성/수정
에디터 본문의 파일 첨부는 
![파일이름.확장자, 로컬인덱스]
로 전달한다.

첨부된 파일의 정보는 FileItemModel로 정의하여 별도로 전송한다.



2. 보기
백엔드에서는 파일업로드 처리를 하고 다음과 같이 수정한다.
![GUID로 생성된 이름.확장자]

//파일인 경우
![file:GUID로 생성된 이름.확장자]

첨부된 파일의 정보는 FileItemModel로 정의하여 전달된다.

프론트엔드에서 '![file:GUID로 생성된 이름.확장자]'와 FileItemModel리스트를 매칭시켜 UI에 표시해준다.